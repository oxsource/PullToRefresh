package com.pizzk.refresh;

import android.content.Context;
import android.util.AttributeSet;

import pizzk.android.ptr.view.RefreshAttach;

public class NewRefreshHeader extends RefreshAttach {

    public NewRefreshHeader(Context context) {
        super(context);
    }

    public NewRefreshHeader(Context context, AttributeSet attrs) {
        super(context, attrs);
    }
}
